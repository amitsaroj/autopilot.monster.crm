import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function ImportContactsPage() {
  return <PagePlaceholder title="Import Contacts" description="Bulk import contacts via CSV or XLSX" backHref="/crm/contacts" backLabel="Contacts" />;
}
