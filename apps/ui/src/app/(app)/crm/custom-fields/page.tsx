import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function CustomFieldsPage() {
  return <PagePlaceholder title="Custom Fields" description="Define custom fields for entities" />;
}
