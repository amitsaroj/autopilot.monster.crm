import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function InboxSettingsPage() {
  return <PagePlaceholder title="Inbox Settings" description="Routing, SLAs, canned responses, tags" backHref="/inbox" backLabel="Inbox" />;
}
