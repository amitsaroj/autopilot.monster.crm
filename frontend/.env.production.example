# Frontend Production Environment — EXAMPLE ONLY
# Copy to frontend/.env.production and fill real values. Do NOT commit secrets.
NEXT_PUBLIC_API_URL=https://api.autopilots.monster/api/v1
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_or_test_placeholder
