import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function ImportLeadsPage() {
  return <PagePlaceholder title="Import Leads" description="Bulk import leads via CSV" backHref="/crm/leads" backLabel="Leads" />;
}
