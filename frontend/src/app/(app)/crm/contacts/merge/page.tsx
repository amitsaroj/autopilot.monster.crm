import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function MergeContactsPage() {
  return <PagePlaceholder title="Merge Contacts" description="Detect and merge duplicate contacts" backHref="/crm/contacts" backLabel="Contacts" />;
}
