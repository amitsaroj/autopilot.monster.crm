import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function WhatsAppBroadcastPage() {
  return <PagePlaceholder title="Broadcast" description="Send broadcast messages to multiple contacts" backHref="/whatsapp" backLabel="WhatsApp" />;
}
