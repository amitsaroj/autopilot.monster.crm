import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function AnalyticsExportPage() {
  return <PagePlaceholder title="Export Data" description="Export analytics data as CSV, PDF, Excel" backHref="/analytics" backLabel="Analytics" />;
}
