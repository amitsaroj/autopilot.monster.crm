import { PagePlaceholder } from '@/components/ui/page-placeholder';
export default function AdminLimitsPage() {
  return <PagePlaceholder title="Limits & Runtime" description="Per-plan limits and runtime config" backHref="/admin" backLabel="Admin" />;
}
