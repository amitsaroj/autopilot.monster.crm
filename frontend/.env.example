# Frontend Application Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Google/Social Login Redirects (Backend handles the actual OAuth)
# These are used for generating dynamic auth links on the client.
